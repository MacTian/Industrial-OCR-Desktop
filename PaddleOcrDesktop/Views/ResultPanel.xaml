<!-- Views/ResultPanel.xaml -->
<UserControl x:Class="PaddleOcrDesktop.Views.ResultPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 统计信息 -->
        <StackPanel Grid.Row="0" Margin="4">
            <TextBlock Text="{Binding ResultViewModel.StatusMessage}" TextWrapping="Wrap"/>
            <Separator Margin="0,4"/>
        </StackPanel>

        <!-- 结果列表 -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding ResultViewModel.Regions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="编号" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="文本" Binding="{Binding Text}" Width="*"/>
                <DataGridTextColumn Header="置信度" Binding="{Binding Confidence, StringFormat=P1}" Width="60"/>
                <DataGridCheckBoxColumn Header="通过" Binding="{Binding IsValid}" Width="45"/>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsValid}" Value="False">
                            <Setter Property="Background" Value="#FFEEEE"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- 识别中遮罩 -->
        <Grid Grid.RowSpan="2" Visibility="{Binding ResultViewModel.IsRecognizing, Converter={StaticResource BoolToVis}}">
            <Grid.Background>
                <SolidColorBrush Color="White" Opacity="0.7"/>
            </Grid.Background>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="150" Height="16"/>
                <TextBlock Text="识别中..." Margin="0,8,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
