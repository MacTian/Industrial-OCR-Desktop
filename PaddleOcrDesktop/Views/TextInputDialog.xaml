<!-- Views/TextInputDialog.xaml -->
<Window x:Class="PaddleOcrDesktop.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入标注文本" Width="400" Height="160"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding PromptText}" Margin="0,0,0,8" FontSize="13"/>
        <TextBox Grid.Row="1" x:Name="InputTextBox" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="4"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="确定" Width="80" Height="28" Margin="4,0,0,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="取消" Width="80" Height="28" Margin="4,0,0,0" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
